# =========================
# Datasource PostgreSQL
# =========================
spring.datasource.url=jdbc:postgresql://localhost:5432/turmasdb
spring.datasource.username=turmas
spring.datasource.password=turmas123

# =========================
# JPA / Hibernate
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Dialect é detectado automaticamente nas versões atuais, mas pode forçar:
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =========================
# Web
# =========================
server.port=8080